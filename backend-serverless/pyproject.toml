[project]
name = "storyteller-backend-serverless"
version = "0.1.0"
description = "StoryTeller Azure Functions backend - FastAPI ASGI, queue worker, SQLAlchemy async, PostgreSQL, Claude"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "azure-functions>=2.2.0",
    "azure-storage-blob>=12.24.0",
    "azure-storage-queue>=12.12.0",
    "aiohttp>=3.14.3",
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.3",
    "sqlalchemy[asyncio]>=2.0.51",
    "asyncpg>=0.30.0",
    "alembic>=1.19.1",
    "pydantic>=2.9.2",
    "pydantic-settings>=2.15.0",
    "email-validator>=2.2.0",
    "pydantic-ai-slim[anthropic,openai]>=2,<3",
    "jinja2>=3.1.4",
    "pyjwt[crypto]>=2.13.0",
    "httpx>=0.28.0",
    "argon2-cffi>=23.1.0",
    "python-multipart>=0.0.18",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "aiosqlite>=0.20.0",
    "mypy>=2.3.0",
    "ruff>=0.16.3",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "W", "N"]

[tool.mypy]
python_version = "3.13"
strict = true
disallow_untyped_decorators = false
plugins = ["pydantic.mypy"]
